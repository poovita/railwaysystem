@model IEnumerable<Rail.Models.track_priority>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Priorities)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.track_status)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Priorities)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.track_status)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Priorities }) |
            @Html.ActionLink("Details", "Details", new { id=item.Priorities }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Priorities })
        </td>
    </tr>
}

</table>
